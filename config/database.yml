default: &default
  adapter: postgresql
  database: sharebuy_prod
  pool: 5
  host: localhost
  timeout: 5000

development:
  <<: *default
  database: sharebuy_dev

test:
  <<: *default
  database: sharebuy_test

production:
  host: <%= ENV["DATABASE_URL"] %>
